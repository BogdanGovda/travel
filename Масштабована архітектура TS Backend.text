https://www.youtube.com/watch?v=rv4LlmLmVWk


Базова теорія
Розділяйте API, application та infrastructure шари для керованого росту системи.
Використовуйте структуровані логи, метрики і трейсинг для production-дебагу.
Проєктуйте ідемпотентні фонові задачі та політики retry.
У складних графах сервісів віддавайте перевагу явному dependency injection.
Обробляйте graceful shutdown і коректне завершення in-flight запитів.


Повна теорія з прикладами коду
Шарова архітектура на практиці
Структура за шарами: src/controllers, src/services, src/repositories, src/domain. Або за фічею: src/users, src/orders — кожна фіча містить всі шари.

Структура за фічами краще масштабується для великих команд. За шарами — простіше для малих проектів.

Ніколи не імпортуйте з вищого шару в нижчий. Domain нічого не імпортує з проекту.

Структура проекту за фічами
// Feature-based structure (recommended for growing projects)
src/
├── shared/
│   ├── errors.ts          // AppError, NotFoundError, etc.
│   ├── middleware/
│   │   ├── auth.ts
│   │   ├── errorHandler.ts
│   │   └── validateBody.ts
│   └── types.ts
│
├── users/
│   ├── user.entity.ts     // domain: User class/interface
│   ├── user.repository.ts // infrastructure: PgUserRepository
│   ├── user.service.ts    // business logic
│   ├── user.controller.ts // HTTP layer
│   ├── user.dto.ts        // Zod schemas: CreateUserDto, UserResponse
│   └── user.test.ts       // tests
│
├── orders/
│   ├── order.entity.ts
│   ├── order.repository.ts
│   ├── order.service.ts
│   ├── order.controller.ts
│   └── order.dto.ts
│
└── app.ts                 // Express setup, middleware, route mounting
Фонові завдання з BullMQ
BullMQ використовує Redis як сховище завдань. Завдання постійні — переживають перезапуски сервера.

Визначайте окремі черги для кожного типу завдань: emailQueue, reportQueue. Дозволяє незалежне масштабування workers.

Налаштуйте retry з exponential backoff: після збою чекайте довше перед кожним повтором.

BullMQ черга та worker
import { Queue, Worker, QueueEvents } from "bullmq";

const connection = { host: "localhost", port: 6379 };

// Define queue
export const emailQueue = new Queue("emails", { connection });

// Add job to queue (from route handler)
async function sendWelcomeEmail(userId: string, email: string) {
  await emailQueue.add(
    "welcome-email",
    { userId, email },
    {
      attempts: 3,
      backoff: { type: "exponential", delay: 2000 },
      removeOnComplete: 100,
      removeOnFail: 200,
    },
  );
}

// Worker (separate process or module)
const emailWorker = new Worker(
  "emails",
  async (job) => {
    const { userId, email } = job.data;

    if (job.name === "welcome-email") {
      await emailService.send({
        to: email,
        subject: "Welcome!",
        template: "welcome",
      });
    }
  },
  {
    connection,
    concurrency: 5,  // process 5 jobs simultaneously
  },
);

emailWorker.on("failed", (job, err) => {
  console.error(`Job ${job?.id} failed:`, err.message);
});
Graceful shutdown для Node.js серверів
Без graceful shutdown активні запити різко обриваються при деплоях, спричиняючи помилки для поточних юзерів.

На SIGTERM: зупиніть прийом нових з'єднань, чекайте завершення активних запитів, закрийте з'єднання з БД, виходьте.

Встановіть максимальний таймаут закриття (30 секунд). Якщо запити не завершуються — примусово виходьте.

Реалізація graceful shutdown
import { createServer } from "http";
import { prisma } from "./db";
import { redis } from "./cache";
import { app } from "./app";

const server = createServer(app);
const SHUTDOWN_TIMEOUT_MS = 30_000;

async function shutdown(signal: string) {
  console.log(`Received ${signal}. Starting graceful shutdown...`);

  // 1. Stop accepting new connections
  server.close(async () => {
    console.log("HTTP server closed");

    try {
      // 2. Close database connections
      await prisma.$disconnect();
      await redis.quit();
      console.log("Database connections closed");
      process.exit(0);
    } catch (err) {
      console.error("Error during shutdown:", err);
      process.exit(1);
    }
  });

  // 3. Force exit if timeout exceeded
  setTimeout(() => {
    console.error("Shutdown timeout exceeded. Forcing exit.");
    process.exit(1);
  }, SHUTDOWN_TIMEOUT_MS);
}

process.on("SIGTERM", () => shutdown("SIGTERM"));
process.on("SIGINT",  () => shutdown("SIGINT"));

server.listen(env.PORT, () => {
  console.log(`Server listening on port ${env.PORT}`);
});
Observability: логування, метрики та трасування
Структуроване логування (JSON формат) вмикає агрегацію та запити в Datadog, Loki або CloudWatch.

Включайте correlation ID в кожен рядок логу для трасування запиту через кілька сервісів.

Health check ендпоінти (/health, /ready) дозволяють load balancers виявляти нездорові інстанси.

Структуроване логування з pino
import pino from "pino";
import { randomUUID } from "crypto";
import { Request, Response, NextFunction } from "express";

export const logger = pino({
  level: process.env.NODE_ENV === "production" ? "info" : "debug",
  formatters: {
    level: (label) => ({ level: label }),
  },
  timestamp: pino.stdTimeFunctions.isoTime,
});

// Request context middleware
export function requestContext(req: Request, res: Response, next: NextFunction) {
  const requestId = (req.headers["x-request-id"] as string) ?? randomUUID();
  req.log = logger.child({ requestId, path: req.path, method: req.method });
  res.setHeader("X-Request-ID", requestId);

  const start = Date.now();
  res.on("finish", () => {
    req.log.info({ statusCode: res.statusCode, durationMs: Date.now() - start }, "Request completed");
  });

  next();
}

// Health check endpoints
app.get("/health", (req, res) => {
  res.json({ status: "ok", uptime: process.uptime() });
});

app.get("/ready", async (req, res) => {
  try {
    await prisma.$queryRaw`SELECT 1`;
    await redis.ping();
    res.json({ status: "ready", db: "ok", cache: "ok" });
  } catch (err) {
    res.status(503).json({ status: "not ready" });
  }
});
CI/CD pipeline для TypeScript backend
CI pipeline валідує кожен коміт: перевірка типів, lint, тести, збірка. Збій будь-якого кроку блокує мерж.

Запускайте міграції БД в CI проти тестової БД перед інтеграційними тестами.

Docker multi-stage builds: build stage компілює TypeScript, production stage копіює тільки dist і node_modules.

Dockerfile для TypeScript backend
# Multi-stage Dockerfile for TypeScript Node.js backend

# Stage 1: Build
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci --include=dev
COPY tsconfig.json .
COPY src/ ./src/
RUN npm run build          # outputs to /app/dist

# Stage 2: Production
FROM node:20-alpine AS production
WORKDIR /app
ENV NODE_ENV=production

COPY package*.json ./
RUN npm ci --omit=dev      # production deps only (~70% smaller)

COPY --from=builder /app/dist ./dist

# Don't run as root
RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser

EXPOSE 3000
HEALTHCHECK --interval=30s --timeout=5s CMD wget -qO- http://localhost:3000/health || exit 1

CMD ["node", "dist/index.js"]

# .dockerignore:
# node_modules
# src
# .env
# .git
# *.test.ts



Пояснення до кожного питання
1
Яке поле є критичним у structured logs для tracing?
Фокусуйтеся на базовому reasoning та практичних прикладах.
Контекст питання: Структуровані логи мають включати:
Підказка: Трасованість.
2
Яка трійка формує базову модель observability?
Фокусуйтеся на базовому reasoning та практичних прикладах.
Контекст питання: Observability складається з:
Підказка: Три ключові складові.
3
Що має включати retry-логіка фонових задач?
Фокусуйтеся на базовому reasoning та практичних прикладах.
Контекст питання: Retry-політика фонових задач має враховувати:
Підказка: Безпечні повтори.
4
Що має робити graceful shutdown?
Фокусуйтеся на базовому reasoning та практичних прикладах.
Контекст питання: Graceful shutdown має робити:
Підказка: Контрольоване завершення.
5
Навіщо використовувати dependency injection у backend-сервісах?
Фокусуйтеся на базовому reasoning та практичних прикладах.
Контекст питання: Dependency injection у backend допомагає:
Підказка: Слабка зв'язність.
6
Реалізуйте createOrder(repo, input) як application use case. Тримайте функцію зосередженою на бізнес-логіці й не прив'язуйте її до HTTP-рівня.
7
Полагодьте handler(run), щоб controller adapter перетворював use-case помилки на HTTP-зручний формат. Поверніть status і body як у разі успіху, так і в разі помилки.
8
Створіть buildServices(env) з явним return type Services. Функція має збирати залежності без витоку деталей реалізації назовні.

